# Изменения

Заметные изменения проекта записываются здесь.

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
нумерация — [семантическое версионирование](https://semver.org/lang/ru/).
Библиотеки у проекта нет, ломать в нём нечего, поэтому мажорную цифру
здесь меняет не сигнатура функции, а смена того, ЧЕМ приложение является
для человека.

**Как вести дальше.** Всё, что сделано после последнего выпуска,
дописывается в раздел «Не выпущено» — в тот же день, что и работа, пока
помнится, зачем она делалась. В день выпуска раздел переименовывается в
версию с датой, под ним заводится новый пустой «Не выпущено», а внизу
дописывается ссылка на сравнение версий. Писать стоит о том, что человек
заметит на экране или в своих данных: перестановка файлов и правка
комментариев сюда не идут.

## [Не выпущено]

### Добавлено

- **Норму недели можно назвать своим числом.** В анкете стояли три
  готовых основания — 40, 36 и 35 часов, — и это ровно те три, что даёт
  закон. Но у части людей неделя короче по причине, которой в списке нет и
  быть не может: работа на неполной ставке, справка, совмещение. Им
  приходилось выбирать «40 ч» и жить с нормой, которая на весь год
  завышена, — то есть с переработкой, которой нет.

  Теперь в списке четвёртое положение — «Своя», и под ним поле для числа.
  Оно перекрывает основание из закона, но не пускает выше сорока: часть 2
  статьи 91 ТК РФ — потолок, а не умолчание, и при 48 поле объясняет это
  вслух и оставляет прежнюю норму. Поле при этом не исчезает под рукой,
  пока человек правит число: выбор читается из того, что своё число
  названо, а не из того, годное ли оно. При возврате к любому основанию из
  закона своё число снимается само, чтобы два ответа не спорили.

  Замер: 30 часов в неделю опускают норму периода с 1972 до 1478 часов.
  Старые профили — без своего числа — читаются ровно как прежде.

- **«Начало отсчёта» стоит во «Внесённых изменениях».** Дата, с которой
  человек ведёт учёт, обрезает период слева и меняет норму сильнее любой
  отдельной правки, но во вкладке с правками её не было: список говорил об
  отпусках и вызовах, а самое крупное решение в нём не значилось. Теперь
  строка стоит там же, что и остальные, ведёт нажатием в анкету и
  снимается крестиком, как всё прочее.

### Исправлено

- **Переход на расчёт с главной больше не ругается в консоли.** Кнопка
  «Открыть график» ведёт на расчёт без перезагрузки страницы, и разметку в
  этот момент рисует React, а не разборщик. Сценарий, ставящий нынешний
  месяц под полосу с числами, стоял внутри заглушки — то есть был тегом,
  который при таком переходе не выполняется никогда, о чём React и говорил
  ошибкой в консоли. Теперь он стоит в раскладке, которая между страницами
  не перерисовывается, а на переходе месяц ставит на место сам расчёт.

- **Отпуск, начатый в сутках после заступления, не отмечался на графике.**
  Смена идёт с 10-го и кончается в 8 утра 11-го; человек отмечает отпуск с
  11-го — и клетка 11-го не меняется никак: ни цветом, ни буквой. Норма при
  этом падает на восемь часов, а переработка на столько же растёт, то есть
  расчёт отпуск видит, а сетка о нём молчит. Читалось это однозначно: «не
  сработало».

  Теперь такие сутки помечены отпуском вполголоса — тем же приглушённым
  цветом и пунктиром, что и свободные сутки внутри отпуска. Часы хвоста
  остаются в клетке числом: они отработаны и посчитаны, и подменить их
  буквой значило бы стереть с сетки восемь часов, которые в итоге есть.
  Своя запись суток отпуску по-прежнему не уступает: пропущенная смена
  остаётся буквой, вызов — кодом.

- **Заглушка на телефоне больше не стоит январём.** Расчёт открывается на
  нынешнем месяце, а заглушка до него — на январе, и в миг подстановки
  страница переезжала под человеком на восемь экранов вниз. Ровно то, ради
  чего заглушка и заведена, она же и ломала.

  Теперь на нынешний месяц встаёт и заглушка — тем же кодом и до первой
  отрисовки, пока браузер разбирает страницу. Прокрутка от этого одна на
  двоих: замер на 320, 390, 430 и 767 точках даёт одно и то же число до и
  после подстановки. Если человек оставил экран на позапрошлом году,
  заглушка не двигается вовсе: учётный год она читает из сохранённого
  профиля.

### Изменено

- **Тёмная тема — теперь умолчание.** Приложение нарисовано тёмным: бумага
  цвета остывшего графита, лампа над страницей, блик по верхней кромке у
  каждой поднятой поверхности. Светлая тема — честный перевод этого языка,
  но именно перевод, и впервые открывший сайт видел его по настройке
  системы. Теперь видит подлинник. Системная настройка никуда не делась:
  «Как в системе» по-прежнему третье положение переключателя в подвале, и
  выбранная однажды тема остаётся выбранной.

- **Окно времени закрывает за собой и окно суток.** Человек отмечает
  отпуск, окно спрашивает «по какое число», он отвечает и нажимает
  «Готово» — а дальше ему приходилось закрывать вручную ещё и окно суток,
  которое всё записало по ходу правки. На телефоне это два прицельных
  попадания подряд. Теперь «Готово» закрывает оба; крестик и Esc, как жест
  «назад», по-прежнему возвращают в сутки.

- **Виды суток в окне дня отмечаются флажками, а не тумблерами.** Тумблер
  отвечает на вопрос «включить ли режим», а здесь вопрос другой — «отметить
  ли»: перед человеком список из двенадцати видов, и он отмечает те, что
  были. Двенадцать тумблеров в столбец читались как двенадцать независимых
  механизмов.

- **Значок настроек — обычная шестерёнка.** Прежний (ползунки) читался как
  «настройки показа», а за кнопкой стоит анкета профиля.

- **Закладки настроек остаются на виду, а анкета уходит под них дымкой.**
  «Настройки профиля» и «Внесённые изменения» уезжали вверх вместе с
  содержимым, и человек, долиставший анкету до конца, переключался обратно
  вслепую: сначала прокрутить наверх, потом нажать. Теперь полоса закладок
  держится у верхней кромки окна всю прокрутку — замер на 390 и 1280
  точках: 8 точек от края и до, и после прокрутки до самого низа.

  Уходит содержимое под неё так же, как под шапкой расчёта: не обрывается
  о кромку, а растворяется — теряет чёткость и уходит в цвет бумаги. И
  щели над полосой больше нет. Щель была настоящая, в восемь точек:
  закреплённой полосе не встать выше верхнего поля тела окна, и в этот
  просвет между заголовком «Настройки» и закладками было видно, как едет
  анкета. Дымка достаёт и туда.

- **Длинные строки во «Внесённых изменениях» сокращаются многоточием.**
  Название вида вместе с датами не помещалось в узкий экран и толкало окно
  вбок. Теперь строка обрезается по ширине окна, а целиком её по-прежнему
  читает программа чтения с экрана.

## [2.15.0] — 2026-09-02

Рабочий экран перестал моргать при обновлении и научился открываться там,
где человек и смотрит: на телефоне под полосой с числами встаёт нынешний
месяц, а управление сеткой переехало вниз, под большой палец. Настройки
разошлись на две закладки, окно дня сохраняет сразу и читается как легенда,
у видов появился «Вызов» без уточнения, а знаки в ряду управления перестали
быть тремя одинаковыми календариками.

### Добавлено

- **Нижняя панель на телефоне.** Управление сеткой — что показывать, за
  какой период и считать ли по сегодняшний день — стояло строкой над
  сеткой. На настольном экране это верно, а на телефоне сетка длиной в
  двенадцать экранов, и строка видна ровно до первого движения пальцем:
  человек, долиставший до октября, чтобы переключиться на календарь, должен
  был вернуться на самый верх, нажать и снова долистать до октября.

  Теперь до 768 точек эти три органа управления стоят внизу экрана, всегда
  под большим пальцем и всегда на месте — и ровно в тех же границах, что
  полоса с числами наверху: от поля до поля страницы. Подложки у панели
  нет: кнопки стоят прямо на бумаге и выглядят ровно как кнопки шапки — та
  же высота, то же скругление, тот же свет лампы по кромке. Что выбрано,
  говорит подъём. Сетка, уезжая под панель, не обрезается, а растворяется —
  как строки под шапкой.

  На экранах уже 330 точек «Календарь» в ней сокращается до «Кал-рь»: слово
  вдвое длиннее соседних и упиралось в края ячейки. Программа чтения при
  этом произносит его целиком.

- **На телефоне расчёт открывается на нынешнем месяце.** Год на телефоне —
  это двенадцать экранов подряд, и человек, зашедший в сентябре, начинал с
  января и листал восемь экранов до того места, ради которого пришёл.
  Каждый раз. Теперь нынешний месяц встаёт верхним краем вплотную под
  полосу с числами, и весь экран под ней занят им одним — от первого числа
  до последнего. Только при открытии: переключение сетки или периода
  страницу под человеком не двигает. На широком экране год виден почти
  весь, и там ничего не прыгает.

- **Знак у режима «Онлайн» вместо тумблера.** В панели тумблер выглядел
  вставкой из настроек и отнимал у подписи половину ячейки. Теперь режим
  показывает знак: выключено — пустое кольцо, включено — залитая точка, от
  которой расходятся волны, и всё это зелёным, тем же, каким на сетке
  отмечены смены. При нажатии волны выезжают из точки и разбегаются
  наружу — нажатие видно движением, а не только сменой цвета. В настройках
  тумблер остался прежним.

- **Вызов без уточнения.** В работе помимо графика были только названные
  случаи: соревнования, сбор, резерв, праздник, выборы. Человека вызывают и
  просто так — подменить, помочь, доделать, — и такой выход приходилось
  записывать чужим видом, лишь бы часы попали в расчёт. Разбор с
  руководителем от этого превращался в разговор о «резерве», которого не
  было. Теперь в списке первым стоит «Вызов», в клетке он помечен «ВЗ».

### Изменено

- **Список видов в окне дня стал легендой.** Под каждой строкой стояла
  плашка, и двенадцать плашек читались как двенадцать отдельных блоков —
  список выглядел длиннее самого себя. Теперь строка та же, что в легенде
  под сеткой: клетка, слово и ничего больше; рамка появляется только под
  указателем. Выключенные тумблеры от этого не потерялись — их дорожка
  стала утопленной, а такой на карточке она темнее любой другой
  поверхности.

  Ушла и оговорка под списком — «часы смены идут в отработанное», «норму не
  трогают». Она менялась на каждое нажатие, а стояла под двенадцатью
  строками, то есть на телефоне за нижним краем: человек нажимал и не видел
  ничего.

  Две колонки теперь появляются с 424 точек вместо 480 — на пятьдесят шесть
  раньше. Порог замерен по самой длинной подписи: «Учебный отпуск» просит
  90 точек, и столько при двух колонках остаётся ровно с 424. Отыграны они
  полем строки, просветами и подписью «Смена в этот день», ставшей
  «Сменой», — заголовок группы и так говорит «смена по графику».

- **Перечень внесённых изменений больше не прокручивается впустую.**
  Переключаясь с анкеты профиля, окно запоминало её высоту, чтобы не
  съёживаться под перечнем из трёх строк. Запоминало оно, однако, высоту
  всего свитка, а не того, что видно, — и человек, открывший перечень,
  прокручивал под ним экран пустоты. Теперь запоминается высота
  показанного, и перечень не прокручивается, когда прокручивать нечего.

- **Три знака управления сеткой стали различимы.** График, календарь и
  период были календариками из одного набора: листок с сеткой, листок с
  шестерёнкой, листок с полосками. В ряду шириной с ладонь они читались как
  один значок, повторённый трижды, и различить их можно было только по
  подписи — то есть значки не работали вовсе.

  Теперь у графика смен стоит знак сайта — четыре клетки цикла с одной
  залитой, — потому что это и есть то, что нарисовано на сетке; у
  производственного календаря — листок с числами, тот самый, что раньше
  занимал график; у периода — часы с тающим следом: отрезок времени, а не
  день, и единственный из трёх без клеток и листка. Поменялось это разом на
  всех экранах.

  Размер у всех троих теперь один. Рисунки пришли из разных мест и занимали
  свою клетку по-разному: замер краски давал 18,3 точки у часов, 17,4 у
  календаря и 19,5 у знака сайта — в ряду они стояли ступенькой. Знаки
  приведены к часам.

- **Режим «Онлайн» — знаком и на широком экране.** Дорожка с кружком
  осталась только в настройках, где рядом такие же строки «вопрос —
  ответ». Над сеткой на её месте теперь та же кнопка, что в нижней панели
  телефона: знак, который выключенным показывает пустое кольцо, а
  включённым — зелёную точку с расходящимися волнами. Человек, повернувший
  телефон, находит на этом месте не другую деталь, а ту же самую.

- **Подпись месяца читается как перечень, а не как дробь.** Величины в ней
  разделяла косая: «8 см / 192,0 ч / ноч. 64,0». Косая в тексте значит «или»
  либо деление, и ночные часы выглядели поделёнными на что-то. Теперь между
  ними вертикальная черта — тот же знак, что в названии приложения между
  цифрами графика, — и она всегда самого тихого тона, даже когда стоит
  рядом с красным числом пропущенных смен. Заодно ушёл нулевой хвост у
  целых часов: «192 ч» вместо «192,0 ч».

- **Слово в подписи календаря согласовано с числом.** Было «1 праздничных»
  и «21 рабочих». Форма зависит не от последней цифры, а от последних двух —
  «21 рабочий», но «11 рабочих», — и правило теперь одно на всё приложение.

- **Названия видов укорочены.** «Дополнительный отпуск» стал «Доп.
  отпуском», «Отгул за переработку» — «Отгулом», «Праздничное мероприятие» —
  «Праздником». Подписи эти стоят в столбце шириной с клетку, и длинные
  занимали там по две строки: список из двенадцати видов читался вдвое
  дольше, чем в нём пунктов. Сокращено только то, что от сокращения не
  теряет смысла: другого отгула, кроме как за переработку, в приложении
  нет, а за что он даётся, сказано в пояснении под выбором.

  **Сохранённые профили от этого не пострадали.** Правились только
  названия — ключи в файле профиля те же, и старый файл открывается вместе
  со всем внесённым.

- **Из легенды ушла строка «Несколько выходов в сутки».** Она объясняла не
  вид клетки, а её крайний случай — два кода рядом вместо одного, — но
  стояла в списке видов наравне с ними, и человек искал среди них шестой
  вид работы, которого нет.

- **Из легенды ушла подсказка про перенос смены.** Три строки текста в
  списке значков объясняли то, что человек и так делает пальцем.

- **Подпись «Онлайн» набрана как «Масштаб».** Она стоит с ним в одном ряду,
  и ряд этот — не текст, а органы управления: узкая гарнитура, прописные,
  приглушённый тон. Обычным текстом строчными она читалась там подписью из
  другого места. В настройках, где рядом стоят обычные строки «вопрос —
  ответ», она осталась прежней.

- **Приложение перестало называть себя калькулятором для одного графика.**
  Оно начиналось как расчёт для суток через трое — отсюда имя и домен, — но
  считает любой сменный: два через два, сутки через четверо, пятидневку и свой
  цикл. В заголовках, описаниях и карточке для пересылки стояло старое: «норма
  и переработка при графике сутки через трое». Человек на два через два,
  увидев такое в выдаче, проходил мимо приложения, которое ему подходит.

  Теперь заголовок говорит о сменном графике вообще, а сами графики названы
  поимённо в описаниях и в карточке для пересылки. Имя сервиса при этом
  осталось прежним: по нему приходят те, кто уже знает, куда идёт. Тексты
  самих страниц не тронуты.

- **Карточка ссылки нарисована шрифтами и цветами сайта.** Она набиралась
  системным гротеском, а знак был перерисован квадратами «на глаз» — три
  залитых вместо трёх обведённых. Ссылка в чате разворачивалась в карточку
  другого сайта. Теперь знак тот же самый, до десятой доли, гарнитуры те же
  (узкая в названии, моноширинная в числах), плашка итога — одна на три
  числа, с бликом от лампы, как над сеткой. И графики в подписи названы
  словами все до одного: раньше два было словами, а пятидневка цифрами.


### Исправлено

- **Календарь больше не моргает при обновлении страницы.** Человек видел
  подряд три состояния: заглушку, пустое место, календарь. Пустое место
  бралось из проступания: сетка появлялась прозрачной и набирала плотность
  за четверть секунды. Замер по кадрам: заглушка пропадала на 567-й
  миллисекунде, месяц становился виден целиком к 944-й — почти четыреста
  миллисекунд пустоты там, где только что стояла сетка.

  Появления у месяца больше нет вовсе. Заглушка занимает то же место и ту же
  высоту, до пикселя, поэтому расчёт просто оказывается на месте костей: в
  первом же кадре после подмены месяц стоит плотностью 1.00, а яркость
  участка идёт 37.8 → 41.6 без провала.

- **В окне настроек была вторая полоса прокрутки.** Прокручиваться должно
  тело окна — у него для этого всё и стоит, — но и сама коробка окна
  расползалась под содержимым и заводила свою. Замер показывал две
  прокручиваемые области сразу; человек видел две полосы рядом и не знал,
  какую тянуть. Теперь полоса одна, а высота окна поднята с 85 до 92 сотых
  экрана — прокручивать приходится меньше.

### Изменено

- **Из приложения ушли упоминания службы в пожарной охране.** Оснований по
  ФЗ-141 в подсказках больше нет: у видов отсутствий и вызовов остались
  статьи Трудового кодекса, которые применимы к любому сменному графику.
  Заодно ушла и профессиональная лексика — «развод», «караул», «часть»: в
  приложении, которым считают часы по любому графику, они были словами из
  чужого словаря.

### Добавлено

- **Настройки разделены на две закладки.** В окне лежали вещи разной природы:
  ответы анкеты, которые правят редко и по одному, и всё отмеченное на сетках
  за год. Сложенные в один свиток, они мешали друг другу — за анкетой
  приходилось прокручивать перечень, за перечнем анкету.

  Теперь их две: «Настройки профиля» и «Внесённые изменения».

- **«Внесённые изменения» — весь год одним перечнем.** Отпуска, больничные,
  вызовы, переносы и отмены смен, свои часы смены, правки видов дня и заметки
  — по порядку дат, каждая строка со своим значком, тем же самым, что стоит в
  клетке на сетке и в легенде.

  Любую можно убрать прямо отсюда, а нажатие на строку открывает те самые
  сутки — там, где эту правку уже умеют менять целиком.

  Внизу этой же закладки стоит «Сбросить календарь»: он стирает ровно то, что
  перечислено выше, и место ему там, где видно, что именно исчезнет.
  «Удалить профиль» осталось в настройках профиля — оно уносит не отметки, а
  саму анкету.

  При переходе между закладками окно не съёживается: высота показанного
  запоминается и держится нижней границей.

- **В шапке появилась кнопка «Открыть».** Действий у профиля ровно три, и они
  одного рода: настроить нынешний, открыть другой, сохранить нынешний. Два
  стояли в шапке, а третье — выбор файла — было доступно только тому, у кого
  профиля ещё нет: в окне создания. Теперь все три в ряд и в одно нажатие.

  Если с последней правки график не сохранялся в файл, приложение сперва
  спрашивает — и не просто предупреждает, а предлагает сохранить тем же окном,
  что и кнопка «Сохранить». Сохранили — выбор файла откроется следом сам.
  Отдельной «Отмены» у вопроса нет: он не «делать?», а «сначала сохранить?», и
  отказ от него означает не бездействие, а второй путь — «Открыть без
  сохранения» ведёт прямо к выбору файла. Ничего не делать по-прежнему можно
  крестиком и клавишей Esc.

  Если файл уже сохранён, никто ни о чём не спрашивает: выбор открывается
  сразу.

  Ради третьей кнопки подписи на них теперь появляются с 640 точек, а не с
  448. А ниже 360 от названия сайта остаются одни цифры графика — «1|3»,
  «2|2»: они называют график самого человека и потому полезнее слова
  «График», которое он и так знает.

- **Занятая вкладка светится кромкой от лампы.** Переключатели сеток, периода
  и разделов настроек ловят свет так же, как все поднятые поверхности
  приложения. Пустые вкладки не светятся и не должны: они не подняты, а
  утоплены вместе с подложкой, а блик на утопленном — это свет без предмета.

### Исправлено

- **Кнопка на первом экране больше не прыгает при появлении.** Надпись на ней
  решается уже в браузере — по тому, есть ли у человека график, — и у
  возвращающегося «Построить график» через мгновение сменялось на «Открыть
  график». Мгновение это приходилось ровно на появление первого экрана: замер
  показывал ширину 218 точек на 112 мс и 204 на 290 мс, под ещё не сошедшим
  размытием. Четырнадцать точек, отскочившие посреди плавного появления, глаз
  ловит безошибочно — и читает как рывок анимации, хотя анимация ни при чём.
  Теперь под надпись отведено место по самой длинной из возможных, и подмена
  не двигает ничего.

- **Сборка календаря на первом экране стала плавнее вдвое.** У каждой клетки
  на появлении был свой фильтр — размытие и вспышка цвета. Клеток в месяце три
  с лишним десятка, слоёв два, и выходило семь десятков одновременных
  фильтров, которые браузер считает заново каждый кадр.

  Замер по кадрам, семь прогонов вперемешку: было 37 кадров за 1300 мс и 24
  длиннее 25 мс, стало 48 кадров и 12 длинных. Подскок у клетки остался —
  ушла только вспышка цвета на нём.

- **Страница расчёта переставала прокручиваться после правки дня, открытой из
  настроек.** Нажатие на строку в перечне изменений закрывает настройки и
  открывает те самые сутки; закрыв окно суток, человек оставался на странице,
  которую нельзя было прокрутить ничем, кроме перезагрузки.

  Каждое окно запоминало прокрутку при открытии и возвращало при закрытии, а
  вложенные окна возвращали её друг за другом в обратном порядке — последним
  возвращалось «прокрутки нет». Теперь считается не значение, а число открытых
  окон: прежнее состояние запоминается один раз и возвращается один раз.

- **Окно дня сохраняет сразу, без кнопок «Сохранить» и «Отмена».** Обе внизу
  обещали не то. «Сохранить» обещало, что до нажатия ничего не записано, — а
  выключенный тумблер убирал запись из профиля сразу, не дожидаясь никакой
  кнопки. «Отмена» обещала вернуть как было — и не возвращала того, что уже
  убрано. А человек, закрывший окно крестиком или клавишей Esc, терял
  названное молча: на телефоне обе кнопки лежат ниже последнего поля.

  Теперь правка ложится в профиль по мере того, как её вносят, — как и везде
  в приложении. Не пишется только негодное: пустое поле вместо часов, ноль
  часов, дата окончания раньше открытых суток.

- **Окна приведены к виду сайта.** Заголовок набран тем же шрифтом, что знак в
  шапке и подписи в карточках, — прописными и с разрядкой. Линия под шапкой
  убрана: линовка в этом приложении живёт внутри карточек, а не обводит блоки
  снаружи. Поля шапки и тела совпали, крестик стал тише.

  И само окно теперь ловит свет лампы, как всякая поднятая поверхность:
  раньше самая поднятая вещь на экране стояла плоским щитом среди карточек, у
  которых блик есть.

- **Окна снова непрозрачные, а лампа над ними не гаснет.** Окно было
  прозрачным, а страница под ним размывалась. На тёмной теме это ещё
  читалось, на светлой — нет: сквозь окно просвечивала сетка на год, поля
  формы стояли на клетках календаря. Теперь окно устроено так же, как лист на
  телефоне: своя бумага, шапка с линией под ней, прокрутка внутри тела.

  Заодно исчезло родное затемнение у окна поверх окна — то, что гасило вместе
  со страницей и лампу. Свет теперь горит над любым окном, а страница под
  вопросом просто гасится плотнее. Цена названа честно: нижнее окно под
  вопросом не притемняется — снизу до него не дотянуться ничем, кроме того
  самого слоя, что выключал свет.

- **Масштаб снова кнопками.** Ползунок на шести ступенях ловил бегунком то,
  что нажатием делается вернее, и был единственной деталью своего рода в
  строке, где всё остальное — кнопки. Теперь это «крупнее» и «мельче»: та же
  высота, то же скругление и тот же свет лампы по верхней кромке, что у
  переключателя сеток, кнопки периода и кнопок шапки. Упёршись в предел,
  кнопка не исчезает, а бледнеет — видно, что дальше некуда.

- **Заглушка рабочего экрана перестала врать о размере.** Она стоит на месте
  расчёта, пока читается профиль, и обязана занимать ровно то место, которое
  займут числа и сетки, — иначе страница дёргается в момент подстановки.
  Разошлась она в двух местах сразу.

  Внизу висела заглушка блока, которого давно нет: рассказ «Где лежат ваши
  данные» на три абзаца, кнопка выгрузки и удаление профиля. Всё это уехало в
  шапку и настройки ещё раньше, а обещать его заглушка не перестала — страница
  выходила на две с лишним сотни точек длиннее настоящей.

  А сетки месяцев она рисовала своим кеглем: 12 точек против 11,28 у расчёта.
  От этой разницы разъезжалось всё — строка букв недели выше на 1,7 точки,
  клетка на 1,6, — и к декабрю набегало девять точек, которые страница
  отыгрывала рывком. Теперь кегль считает сама таблица стилей, по ширине
  колонки, и он один на заглушку и на расчёт. Замер на 390, 768, 1024, 1280,
  1440 и 1920 точках: положение первой клетки, декабря, подвала и высота всей
  страницы совпадают.

- **Свет лампы включается сразу, а не через полсекунды.** Блик по верхней
  кромке и тень вниз есть у каждой поднятой поверхности — карточек, плашек,
  кнопок, клеток календаря, — но ставила этот свет сама лампа, из сценария.
  Замером: страница показывалась на 217-й миллисекунде, а свет появлялся на
  597-й, ровно тогда же, когда заглушка сменялась расчётом. Выходило, что свет
  включается вместе с подстановкой, одним рывком на весь экран. Теперь он на
  месте с первого кадра — и в заглушке тоже.

- **Сетки в графике и производственном календаре просто проступают.** Они
  собирались волной по клеткам — приём с первого экрана, — и начиналась эта
  сборка ровно в тот миг, когда пропадал скелет. Человек, обновивший страницу,
  видел подряд два разных появления одного и того же, и ожидание от этого не
  сокращалось, а удваивалось. Теперь месяц проступает целиком за четверть
  секунды; заодно вместо пятисот анимаций осталось двенадцать.

## [2.14.0] — 2026-08-29

Календарь собран из клеток-панелей: сутки лежат врозь, каждые со своим бликом
и тенью. Свет заодно дошёл до блоков, что стояли без него, а история первого
экрана стала на секунду короче.

### Изменено

- **Свет лампы дошёл до последних блоков, что стояли без него.** Карточки
  «Полезно знать», вопросы в «Частых вопросах» и показ выгрузки в файл
  держались серой рамкой, пока всё вокруг держится светом: блик по кромке,
  обращённой к лампе, и мягкая тень. Рамка среди освещённых блоков читалась
  чертежом среди предметов.

  В показе выгрузки заодно выстроена та же лесенка тонов, что в настоящем
  окне: окно на бумаге, карточка на тон выше, поле на тон ниже карточки, — а
  поле, как и в приложении, держится заливкой, а не контуром.

- **История первого экрана стала на секунду короче.** Между шагами стояла
  пауза, и следующий начинался после того, как предыдущий полностью
  закончился: отметка отпуска, вдох, отметка отгула, вдох, числа. Четыре
  секунды, из которых последнюю треть человек досматривал уже вхолостую.

  Теперь шаги заходят друг на друга: отметка отгула трогается ровно тогда,
  когда отметка отпуска прошла свой пик и стала оседать, а числа — когда
  начала оседать отметка отгула. Порядок событий читается тот же, а
  хронометраж — три секунды вместо четырёх.

### Исправлено

- **Под отметкой отсутствия просвечивала рамка смены.** Отметка
  полупрозрачна — это её собственный тон, тот же, что в расчёте, — и сквозь
  неё была видна рамка клетки, лежащей ниже. На подскоке, когда отметка
  вырастает крупнее клетки, зелёный контур оказывался ВНУТРИ неё отдельным
  прямоугольником. Теперь рамка гаснет вместе с появлением отметки и к её
  пику исчезает совсем.

- **Свет лампы дошёл до показов на главной.** Полоса чисел на первом экране
  и карточка настроек в показе «Как работает» — те же плашки, что в
  расчёте, но света на них не было: они стояли ровными заливками рядом с
  блоками, у которых блик и тень есть. Теперь есть и у них.

  У полосы чисел тень при этом своя, длиннее общей: она лежит поверх листа
  с календарём, на пару сантиметров ближе к читателю, и это единственное
  место на странице, где тени есть на что упасть. Уходит она вниз и в
  сторону от лампы — туда же, куда и свет.

  Своя тень и у клеток первого экрана: они там вчетверо крупнее, чем в
  расчёте, и общая тень на них не читалась вовсе — замером есть, глазом
  нет. Задана она долями кегля, а не точками, поэтому растёт вместе с
  клеткой на любой ширине окна.

- **Клетка календаря стала маленькой панелью.** Сутки стояли вплотную,
  одной сплошной плашкой на весь месяц, и месяц был фигурой с уступом там,
  где он начинается не с понедельника. Теперь каждые сутки лежат на бумаге
  отдельным квадратом: скругление, блик от лампы по кромке, обращённой к
  ней, и мягкая тень вниз — то же, что у блоков с числами наверху. Между
  клетками просвет.

  Просвет этот — тот самый, что раньше протекал между ними случайной белой
  нитью: семь столбцов делят ширину, которая на семь не делится, границы
  попадают между пикселями, и там, где округления соседей расходились,
  оставался незакрашенный столбец. Его закрывали подложкой в пиксель под
  каждой клеткой. Теперь просвет намеренный и одинаковый, а подкрашивать
  нечего.

  Вместе с плашкой ушло и всё, что её держало: скос вогнутого угла, расчёт
  скруглений по соседям каждой клетки и та самая подложка. Своей заливки у
  месяца при этом нет: месяц и есть эти клетки, а не плашка под ними.

  Наклон блика считается один на месяц, а не на каждую клетку: клетки
  месяца стоят от лампы на одном расстоянии, а мерить их все по отдельности
  — четыреста замеров с перерасчётом раскладки между ними на каждую
  перерисовку.

## [2.13.0] — 2026-08-29

Окно дня стало одним списком: что в этих сутках стоит — видно по тумблерам, а
время у каждого вида спрашивается отдельным окном поверх.

### Изменено

- **В окне дня один список на всё: смена, выходной, освобождение от работы,
  работа помимо графика.** Смена спрашивалась отдельным тумблером выше
  списка, и человек, пришедший отметить отгул, отвечал на два вопроса
  подряд, не понимая, связаны ли они. Связаны: отгул — это и есть «смены не
  было». Теперь вопрос один, и у каждой строки та же клетка, какой этот вид
  суток стоит на сетке, — включая смену и выходной.

- **Тумблер справа показывает, что в этих сутках СТОИТ.** Раньше список
  отвечал на вопрос «что я сейчас выбираю», и то, что в сутках уже
  записано, приходилось искать отдельным разделом ниже. Теперь включённый
  тумблер и есть ответ: включён — стоит. Включённых бывает и несколько —
  отпуск и вызов в одни сутки.

  Он же и убирает: выключенный тумблер у записанного вида — это удаление,
  и происходит оно сразу.

- **Период и часы вызова показываются при правке, а не только при
  добавлении.** Открыв сутки, в которых вызов уже стоит, человек видел
  пустое «ничего не добавлять»: поправить часы или дату окончания было
  нельзя — только удалить и внести заново, набрав всё сначала. Теперь окно
  открывается НА том, что в сутках стоит: тумблер включён, время показано
  его собственное, а «Сохранить» его правит, а не заводит вторую такую же
  запись.

  Открыв середину вызова с 7 по 9 января, человек видит 9 января, а не
  открытые сутки: дата у записи своя.

- **Время спрашивается отдельным окном поверх, а не в самом окне дня.**
  Начало смены и её продолжительность, срок вызова и часы в сутки
  появлялись прямо под списком. Список из двенадцати строк на телефоне уже
  высотой в экран, и поля вырастали за его нижним краем: человек включал
  вызов, на видимой части не менялось ничего — и он шёл сохранять, не
  назвав ни часов, ни срока.

  Теперь они спрашиваются окном поверх окна дня, которое не заметить
  нельзя. Своего «Сохранить» у него нет: на бумагу всё ложится одним
  нажатием внизу, а второе «Сохранить» означало бы, что первое что-то уже
  записало.

  У включённой строки слева от тумблера стоит карандаш — им это окно
  открывается снова, когда время нужно поправить, а не задать впервые. У
  выходного карандаша нет: времени у него не бывает.

- **Несколько вызовов в одни сутки вносятся за одно открытие.** Так и
  бывает: после смены соревнования, а следом резерв, и часы у них разные.
  Включение второго ГАСИЛО первый — на глазах и без объяснения, потому что
  выбор в окне был один на все виды. Теперь у каждого включённого вида своё
  время, и записываются они все разом, по «Сохранить».

## [2.12.0] — 2026-08-29

Вид суток в окне дня выбирается клетками — теми же, что стоят на сетке, — а
примеры в легенде встали сеткой и перестали рассыпаться на телефоне.

### Изменено

- **Вид суток в окне дня выбирается клетками, а не списком.** Список
  показывал одно значение, а остальные десять прятал за нажатием: человек,
  открывший сутки ради отпуска, не знал, что там же лежит отгул и что
  считается он иначе, — пока не раскроет и не прочитает все строки подряд.
  На телефоне раскрытый список вдобавок закрывал собой окно, из которого
  был вызван.

  Теперь все одиннадцать вариантов видны сразу, и каждый показан ТОЙ ЖЕ
  клеткой, какой он потом стоит на сетке: нажал на «О» — увидел «О» в
  календаре. Легенда и правка перестали быть двумя словарями, буквы и цвета
  у них общие — и легенда по-прежнему называет все виды до единого, чтобы
  прочитать сетку можно было, не открывая ни одного дня.

  Под клетками — настоящие переключатели, а не ряд кнопок: с клавиатуры
  выбор идёт стрелками, а не одиннадцатью нажатиями табулятора, и экранный
  диктор называет группу, число вариантов и выбранный.

- **Примеры в легенде встали сеткой.** Раньше они шли переносом строк, и
  каждая строка вставала по своей ширине: подписи в группе разной длины, и
  вторые столбцы у соседних строк не совпадали ни в одной. На телефоне и на
  среднем экране это читалось как рассыпанный текст, а не как список.
  Теперь столбцов два до среднего экрана и три на нём; на широком, где
  легенда стоит колонкой слева, всё как было.

## [2.11.1] — 2026-08-28

Продолжение растворения над кромкой окна наконец рисуется на телефоне: три
выпуска подряд слой был на месте, но прозрачным, а дотянуться до нужной
полосы не мог и будь он видимым.

### Исправлено

- **Над кромкой окна ничего не менялось три выпуска подряд.** Причин
  оказалось две, и обе — в одном слое.

  Первая: он не рисовался вовсе. Заливка была задана градиентом, в который
  доля бумаги передавалась переменной; такое правило браузер либо понимает
  целиком, либо выбрасывает целиком — на телефоне выбросил. Слой оставался
  на месте, верный размером и положением, и совершенно прозрачным. Теперь
  заливка сплошная, а долю задаёт маска — тем же способом, каким сделано
  растворение под шапкой, которое на том же телефоне работает.

  Вторая: закреплённому слою до этой полосы не дотянуться в принципе. У
  телефонного браузера окон два — страничное, в котором считается вёрстка,
  и видимое, то есть то, что между строкой адреса и клавиатурой. Верхняя
  строка полупрозрачна, видимое окно начинается ниже страничного, и в
  промежутке видно страницу. Всё закреплённое браузер держит по кромке
  ВИДИМОГО окна — ровно там, где промежуток кончается.

  Поэтому слоёв теперь два, об одном и том же, но в разных системах
  координат: один по кромке окна, другой по кромке страницы. Высоту
  промежутка браузер сообщает сам, и слой берёт её замером, а не на глаз:
  у сафари, у приложения с домашнего экрана и у встроенного браузера она
  разная и меняется на ходу — строка сжимается при прокрутке.

  На настольном браузере промежутка нет, второй слой уходит целиком выше
  кромки, и там не меняется ничего.

## [2.11.0] — 2026-08-28

Страница получила весь экран телефона: растворение содержимого доходит теперь
до самого его верха, лампа висит под часами и не размывается вместе с ними, а
у значка на домашнем экране появилось лого.

### Исправлено

- **Страница получила весь экран телефона, и растворение дошло до его
  верха.** Два выпуска подряд лечили прореху над лампой снаружи, а
  устроена она была изнутри: браузер отдавал странице прямоугольник МЕЖДУ
  полосой под часами и полосой над чёрточкой, а сами полосы рисовал сам.
  Содержимое при прокрутке в них уезжало и там было видно — а слои
  растворения нет, потому что за пределами окна их не существует. Отсюда и
  резкая черта поперёк экрана: до неё бумага, за ней ничего.

  Теперь окно у страницы — весь экран (`viewport-fit=cover`), полосы
  принадлежат ей, и растворение идёт до самого верха тем же слоем, что и
  под шапкой: на линии лампы от содержимого остаётся десятая, и сходит она
  на нет к кромке экрана. Никакой особой точки на кромке больше нет —
  потому что нет и самой кромки.

  Взамен всё, что стояло по краю, от края отступает: содержимое страницы,
  лист настроек во весь экран и липкая полоса с числами. Шапка отступает
  вместе с содержимым — она стоит по его верхней кромке. Читать текст
  из-под часов нельзя, а вот растворять его там — можно и нужно.

- **Лампа больше не размывается вместе с полосой под часами.** В
  приложении, добавленном на домашний экран, система размывает всё, что
  попадает под эту полосу, — а лампа висела ровно там, и вместе со светом
  расплывался её корпус. Теперь она висит по кромке безопасной зоны, то
  есть сразу под часами: сама чёткая, а выше неё — бумага, размывать
  которую нечего. На настольном браузере лампа стоит там же, где стояла.

- **Слой-продолжение вообще не рисовался в сафари.** Его дно стояло на
  кромке окна долей от высоты окна, а на телефоне окон три — маленькое,
  большое и текущее, — и какое из них возьмёт браузер, не оговорено.
  Сафари взял не то, и слой уехал выше видимой части целиком. Теперь дно
  задано длиной, а длина ни от какого окна не зависит.

- **У значка на домашнем экране появилось лого.** Айфон рисовал вместо
  него чёрный квадрат с буквой: значок сайта лежал в `svg`, а для ярлыка
  айос берёт только растровый `apple-touch-icon`, которого не было вовсе.
  Теперь есть — тот же знак, что в шапке.

  Заодно починен и сам `svg`: в нём была сломана разметка — правка,
  менявшая написание графика с косой черты на палку, прошлась и по
  файлу значка, превратив `w3.org` в `w1/3.org`, а координаты клеток — в
  дроби. Значок не рисовался ни во вкладке, ни в закладках.

## [2.10.2] — 2026-08-28

Растворение содержимого доведено до конца: выше кромки окна оно теперь
продолжается, а не обрывается — и в сафари на телефоне тоже.

### Исправлено

- **Растворение выше кромки окна доведено до конца — и наконец работает.**
  Прошлый выпуск закрывал прореху над лампой сплошной бумагой, и вышло
  дважды не то.

  В сафари на телефоне пропало вообще всё, что выше кромки: и содержимое, и
  дорисованный корпус лампы с проводом. А при смене темы верх оставался в
  прежнем цвете — поперёк экрана ложилась граница между старой бумагой и
  новой, и убрать её можно было только перезагрузкой страницы. В браузере
  телеграма не изменилось ничего: прореха осталась на месте.

  Причина у обоих случаев одна. Слой, которым дымка размывает подложку,
  за кромку окна вылезать не может: движок сафари рисует такой слой за её
  пределами как попало — там он закрыл собой всё и перестал слушаться смены
  темы, тут не нарисовался вовсе. Обычная заливка, без размытия, там
  рисуется как любой другой элемент — лампа тому и доказательство, она
  видна всегда.

  Поэтому слоёв теперь два: дымка с размытием остаётся внутри окна, а выше
  кромки идёт простая заливка. И растворение больше не кончается на кромке
  — на ней остаётся девять десятых бумаги, а последнюю десятую содержимое
  теряет уже выше, на протяжении пары сантиметров. Там, где браузер
  показывает страницу под полупрозрачной верхней строкой, видно именно
  продолжение: содержимого всё меньше, и сходит оно на нет само, а не
  упирается в край.

  Размытия наверху нет, и оно там не нужно: сквозь девять десятых бумаги
  разница между размытым и чётким уже не видна.

## [2.10.1] — 2026-08-28

Прореха над лампой: там, где верхняя строка браузера полупрозрачна, страница
видна и выше кромки окна, а растворять содержимое там было нечем.

### Исправлено

- **Над лампой содержимое снова становилось чётким.** Дымка, в которой
  растворяется всё уезжающее вверх, начиналась ровно на верхней кромке окна
  и там же кончалась сверху. Обычно этого достаточно: выше кромки ничего не
  видно. Но у части браузеров — телеграмного в первую очередь — верхняя
  строка полупрозрачна, и страница под ней просвечивает: ровно там же, где
  у лампы дорисованы корпус и провод.

  Получалась прореха: содержимое доезжало до кромки растворённым, а выше
  снова становилось ярким и чётким — заголовок первого экрана и клетки
  календаря поверх лампы и её провода.

  Теперь растворение продолжается и выше кромки — сплошной бумагой, без
  всякого спуска: там содержимого быть уже не должно вовсе. Цвет у неё тот
  же, что у страницы, поэтому видна дорисовка только тем, что в ней
  пропадает.

  Затемнение под открытым окном вынесено за кромки по тому же поводу: над
  ним оставалась непогашенная полоса содержимого.

## [2.10.0] — 2026-08-28

Над листом зажглась лампа: страница перестала быть ровной заливкой, у неё
появились верх и низ, а свет можно взять и перекрасить. Считать при этом
стало точнее — в настройках появилось начало отсчёта для тех, кто пришёл
на службу не с января, а сегодняшний день на сетке отмечен.

### Добавлено

- **По верхней кромке окна светит лампа.** Страница была ровно залита
  бумагой и от этого читалась плоско: у листа не было ни верха, ни низа.
  Теперь сверху светит трубка, и всё остальное — тёплый налёт на бумаге,
  блик на верхней кромке блоков, мягкая тень под ними — следствие того,
  что она горит.

  Свет намеренно ниже порога, за которым его замечаешь как приём: под
  самой лампой бумага набирает тридцать четыре уровня яркости из 255 в
  тёмной теме и двадцать пять уровней тепла в светлой, а к трети экрана
  от лампы не остаётся ничего.

  Доли на светлой теме крупнее, чем на тёмной, хотя кажется, что должно
  быть наоборот. На графите лампа делает два дела разом — поднимает
  яркость и подкрашивает; на белом первого дела нет вовсе, светлеть
  бумаге некуда, остаётся одна подкраска, а разницу в оттенке глаз ловит
  куда хуже разницы в яркости.

  Лампу можно нажать — она меняет цвет. Их шесть: сперва три белых света —
  тёплый, нейтральный, холодный, — то есть то, чем лампы и бывают на самом
  деле, а следом три цветных: зелёный, синий, розовый. Выбор запоминается в
  браузере, как и тема оформления, и в профиль не пишется: файл с графиком,
  в котором записано «лампа зелёная», — это уже не файл с графиком.

  Пока цвет не выбран, лампа светит по теме: на тёмной зеленоватым — тёмная
  тема этого приложения зелёная по всему, — а на светлой нейтральным.
  Цветной свет на белом это уже не свет, а краска: бумага становится
  зелёной, и лист, на котором человек считает часы, перестаёт быть листом.
  Умолчание следит за темой; после выбора цвета — нет, свой ответ старше
  умолчания.

  При открытии страницы лампа зажигается с двумя короткими миганиями, как
  и положено трубке. Смена цвета — тоже розжиг, и схватывается он не
  всегда с первого раза: обычно сразу, иногда с одной осечки, изредка с
  двух. Раз в минуту-другую свет коротко проседает — не гаснет, а именно
  проседает: это то, чем горящая трубка отличается от нарисованной
  полоски. У того, кто отключил анимацию в системе, лампа просто горит.

  Выше верхней кромки окна у неё дорисован корпус, отходящий вверх провод
  и намотанная на него изолента в две кривые намотки. Обычно этого не
  видно вовсе, но у части браузеров — телеграмного в первую очередь —
  адресная строка полупрозрачна, и страница под ней просвечивает: там
  лампа оказывается лампой целиком.

  Открытое окно её не гасит: свет остаётся гореть над затемнённой
  страницей. Лампа освещает комнату, а не лежит в ней. Исключение одно —
  окно поверх окна: вопрос «стереть год отметок?» гасит комнату целиком,
  включая свет.

- **Содержимое растворяется, уходя под шапку.** У шапки и у полосы с
  числами больше нет заливки, и это правильно: две плашки во всю ширину
  поверх страницы читались как второй слой, наклеенный на первый. Но у
  заливки была работа — гасить то, что проезжает под ними, — и без неё
  сквозь шапку было видно, как уползает вверх название профиля, а между
  плашками с числами — как едут клетки календаря.

  Теперь содержимое не обрывается по линии, а растворяется по мере
  подъёма: теряет чёткость и уходит в цвет бумаги. Под шапкой его ещё
  видно — приглушённо и не в фокусе, — и сходит оно на нет ровно у
  верхней кромки окна. Там, наверху, оно ещё не «за краем экрана» — оно
  просто уже не для чтения.

- **Блики легли с той стороны, с которой светит лампа.** Блик на одной
  верхней кромке верен только для того, что стоит прямо под лампой. Кнопки
  шапки стоят у правого края, дальше конца трубки: до них свет доходит
  сверху и слева, и блик сверху читался бы неправдой — предмет,
  освещённый не с той стороны, глаз ловит быстрее, чем успевает объяснить
  себе, что не так.

  Теперь сторону считает сама лампа, замером: чем дальше блок вынесен за
  конец трубки, тем сильнее свет заходит сбоку, и вместе с ним
  разворачивается тень. Заодно свет ловят кнопки «Настройки» и
  «Сохранить».

- **Начало отсчёта: с какого числа календарь ваш.** Учётный период
  установлен работодателем и начинается с января независимо от того, когда
  человека приняли. Устроившемуся в августе приложение показывало годовую
  норму — почти две тысячи часов, из которых его восемьсот, — и
  «недоработку» на всю разницу. Заведомо неверное число стояло первым и
  крупнее всех.

  Теперь в настройках, сразу за датой смены, стоит «Начало отсчёта».
  Указанная дата обрезает период слева: норма считается по рабочим дням
  внутри отрезка (ст. 104 ТК РФ), а не «за год с вычетом лишнего» — это
  разные числа, и в приказе стоит первое. Сетка начинается там же.

  Это не дата смены и не заменяет её. Дата смены задаёт ЦИКЛ и может быть
  хоть завтрашней — график строится от неё в обе стороны, в том числе на
  годы до трудоустройства. Начало отсчёта отвечает на другой вопрос: с
  какого числа этот график ваш.

  Пустое поле значит «с начала выбранного периода» — то есть ровно то, что
  было раньше. У всех сохранённых профилей оно пустое, и ни один расчёт от
  этого выпуска не изменился.

  Месяц, целиком лежащий раньше начала отсчёта, показывает нули — и
  говорит, почему: ноль на экране расчёта иначе читается как «не
  посчиталось».

- **Сегодняшний день отмечен на сетке красным контуром.** На экране до
  трёхсот шестидесяти пяти одинаковых квадратов, и найти в них себя было
  нечем — «где я сейчас» приходилось искать счётом по неделям. Отметка
  одна на весь год, потому и работает.

  Она стоит в обеих сетках — и в графике смен, и в производственном
  календаре, — потому что человек переключается между ними кнопкой.
  Контуром, а не заливкой и не рамкой: и то и другое уже занято видом
  суток, и отметка, взяв их, стирала бы у сегодняшнего отпуска признак
  «смена не состоялась». Незрячему читателю «сегодня» сказано словом.

### Изменено

- **Масштаб календарей стал ползунком.** Было две кнопки и четыре ступени:
  два, три, четыре и шесть месяцев в ряд. Теперь одна шкала, и тянуть её
  вправо — крупнее, влево — мельче.

  Отметок у неё столько, сколько месяцев помещается в колонку: от двух в
  ряду до восьми на широком мониторе, и ни одной впустую — правый конец
  считается по замеру, а не назначен числом. Сетка при этом на любой
  отметке занимает всю ширину до края, без откушенного правого поля. Кегль
  в клетке идёт следом за размером месяца, но не пропорционально: у мелкого
  масштаба есть порог читаемости.

  Отметки, а не плавный ход, — потому что одновременно плавным и во всю
  ширину масштаб быть не может: пока месяцы делят ширину поровну, размер
  месяца это ширина, делённая на их число, а число целое. Между «четыре в
  ряд» и «три в ряд» ничего нет.

- **История первого экрана стала короче на ход.** Она шла в четыре хода —
  отметка, числа, отметка, числа, — по паре на каждое отсутствие. Пара
  «причина и следствие» читается лучше всего, но на первом экране человек
  не досматривает: он листает дальше, и вторая половина оставалась никому
  не показанной.

  Теперь ходов три, и они отвечают на два разных вопроса, а не на один
  дважды: сперва человек отмечает отпуск, потом отгул, и только потом
  считаются числа. Главное при этом цело — норма как стояла 160, так и
  стоит, а отработанное падает со 192 до 144: двух смен нет, а
  отрабатывать всё равно те же 160.

- **В показе «График и норма» теперь два нажатия, а не одно.** Графиков
  стало три: «сутки через трое» → «два через два» → «пять через два».
  Одного нажатия было мало — оно показывало, что график меняется, но не
  показывало главного: что меняется он на ЛЮБОЙ, и последний из трёх
  устроен вовсе иначе. У пятидневки выходные задаёт производственный
  календарь, а не скользящий цикл, и на полосе это видно без единого
  слова: рабочие клетки впервые встают подряд и упираются в край недели.

  Круг показа стал восьмисекундным вместо шести: два нажатия в шесть
  секунд влезают, но между ними остаётся полсекунды — меньше, чем нужно,
  чтобы прочитать собравшуюся полосу.

- **Указатель в показе перестал закрывать собой график.** Рука стояла
  поверх значения, и на экране было видно «1» и ладонь вместо «1|3» — то
  единственное, ради чего показ и заведён. Теперь палец наезжает на правый
  нижний угол значения, а рука уходит за него.

### Исправлено

- **Вогнутый угол месяца вылезал острым квадратом.** Там, где месяц
  начинается не с понедельника, у его плашки есть вогнутый угол, и он
  сглажен скосом. Скос закрашивал лишнее квадратиком цвета страницы — и
  пока страница была ровно залита, этого хватало. Под лампой она уже не
  ровная, подделка перестала совпадать с подлинником на два-три уровня, и
  вдоль прямого угла это стало видно сразу — ровно там, где скос и
  заведён, чтобы угла НЕ было. Теперь лишнее не закрашивается, а
  вырезается: сквозь скос видно ту самую страницу, что и вокруг.

- **Окна не гасли на айфоне** — исчезали разом, дёрнув содержимым вверх.
  Три выпуска подряд лечили не то. Уход окна был записан одними правилами
  оформления, и среди них `overlay`, которого в Safari нет вовсе. Без него
  окно вылетает из верхнего слоя в тот же кадр, когда закрылось, — уход
  нечем показать в принципе. На настольном браузере всё выглядело гладко,
  поэтому причину и искали не там.

  Теперь уход ведёт скрипт: окно остаётся открытым все сто тридцать
  миллисекунд перехода и закрывается по их окончании.

- **«Сегодня» считалось по Гринвичу, а не по вашим часам.** В Москве с
  полуночи до трёх часов ночи, а на Камчатке с полуночи до полудня
  приложение считало сегодняшним ВЧЕРАШНИЙ день. Отсюда режим «Онлайн»
  обрезал расчёт на сутки раньше — то есть ровно в ночную смену, когда в
  него и смотрят.

## [2.9.2] — 2026-08-27

Две поломки, которые предыдущий выпуск не долечил, а одну и вовсе завёл, —
и шапка листа настроек стала бумагой, как у остальных окон.

### Исправлено

- **Второе открытие настроек показывало кнопку вместо шапки.** Заливка
  застревала скруглённым прямоугольником ровно там и такого размера, какой
  была кнопка настроек, — то есть показывала свой БАЗОВЫЙ вид, из которого
  её и должна была вывести анимация.

  Виноват способ, которым 2.9.1 перезаводил анимации листа: разговор с ними
  напрямую. На настольном браузере он работает, на телефоне оставляет их
  отменёнными навсегда. Теперь анимации гасятся и заводятся меткой на самом
  окне — тем же приёмом, которым заведены показы на посадочной странице.

  Проверено на трёх кругах подряд, в обеих темах, с паузой в шестьдесят
  миллисекунд между закрытием и следующим открытием — то есть внутри того
  окна, когда лист ещё на экране: каждый раз заливка идёт от кнопки до края
  шапки, значок — от кнопки до места знака сайта.

- **Окно подпрыгивало перед тем, как исчезнуть.** Прокрутка страницы
  возвращалась в первый же кадр закрытия, то есть страница менялась под ещё
  видимым окном. На телефоне это заметно: снятая прокрутка держит адресную
  строку раскрытой, возвращённая — отпускает её, высота окна браузера
  меняется, а вместе с ней и та мера, которой лист меряет себя.

  Теперь возврат ждёт конца ухода: пока окно на экране, прокручивать всё
  равно нечего — оно закрывает собой страницу целиком.

### Изменено

- **Шапка листа настроек стала бумагой.** Заливка, растущая из кнопки,
  приходит теперь к цвету бумаги, а не поднятой бумаги: шапку от тела
  отделяет линовка, а не заливка — как и во всех остальных окнах.

## [2.9.1] — 2026-08-27

Починка того, что сломал предыдущий выпуск, и доведённое до конца начатое в
нём же: остальные окна собраны тем же строем, что и настройки.

Цифра заплаточная, а не минорная, хотя на экране заметно и то и другое.
Две трети выпуска — это возвращение к тому, как оно и должно было работать
в 2.9.0, а перестроение окон не вводит нового вида: оно доносит до
остальных окон тот, что там уже был.

### Исправлено

- **Второе нажатие «Настроек» открывало лист готовым**: без роста заливки,
  без летящего значка. Анимации листа заводил сам браузер — тем, что окно
  из скрытого становится показанным, — а с появлением плавного закрытия
  окно остаётся показанным всё время ухода и ещё сколько-то поверх.
  Замером: около ста пятидесяти миллисекунд после нажатия на крестик.
  Человек, нажавший «Настройки» в это окно, видел лист уже собранным.
  Теперь анимации заводятся при каждом открытии, а не по счастливому
  стечению обстоятельств.

- **Окна дёргались при закрытии.** В первый же кадр закрытия прокрутка
  страницы прыгала на девятнадцать точек. Виновато место, оставленное под
  полосу прокрутки: лист во весь экран лез под него, и чтобы у листа не
  было непрокрашенной кромки справа, место убиралось на время показа. От
  этого страница под окном перевёрстывалась дважды — на открытии и на
  закрытии, — и на закрытии другая ширина давала другую высоту, а прокрутку
  сносило. Ровно в тот миг, когда окно гасло. Теперь место не трогается
  вовсе, а лист просто не лезет под него.

### Изменено

- **Остальные окна собраны строем настроек.** Окна оказались одним и тем же
  по устройству — вопрос, ответ, иногда пояснение под ним, — а поля
  расставляли кто во что горазд. Теперь по одному строю собраны сутки,
  выбор периода, сохранение в файл, возврат из файла и подтверждения: поля
  стоят на поднятой бумаге строками под общей рамкой, кнопки — на бумаге
  окна.

  Заодно у окна суток пропали три самодельных пояснения под полями: это и
  есть строка со сноской, и выглядит она теперь так же, как в настройках.

- **У шапки окна больше нет своей заливки**: она заодно с телом, а
  разделяет их линовка.

## [2.9.0] — 2026-08-27

Окна перестали возникать и пропадать рывком, а на телефоне перестали быть
окнами: настройки, сутки и «Создать профиль» открываются во весь экран.
Настройки при этом не появляются, а вырастают из кнопки, которой их
открыли. Заодно график везде пишется тем же знаком, что и в названии
сайта, — «1|3» вместо «1/3».

Цифра минорная: на экране заметно всё перечисленное, но приложение
осталось тем же самым и считает то же самое.

### Добавлено

- **Полноэкранный лист на телефоне.** Ниже `sm` окно шириной в экран минус
  два поля и высотой в 85 процентов — это не окно, а страница с полями:
  полей хватает ровно на то, чтобы показать полоску чужого содержимого по
  краям и отнять у формы четыре строки. Настройки, окно суток и «Создать
  профиль» теперь занимают экран целиком.

  Настройки при этом ВЫРАСТАЮТ из кнопки: её подложка со всеми своими
  скруглениями растягивается, пока не накроет шапку листа; значок — а если
  на кнопке видна подпись, то значок вместе с ней — уезжает на место знака
  сайта и подъезжает к нему с замедлением; знак и кнопки гаснут, под шапкой
  проступает линовка, поля поднимаются. Полноэкранное окно, возникшее
  мгновенно, на телефоне неотличимо от перехода на другую страницу: человек
  видит другой экран и тянется к кнопке «назад» браузера, а она уводит с
  сайта. Переход показывает, что экран не сменился, а раскрылось то, по
  чему нажали.

  Кнопке со страницы расти некуда — до шапки далеко, и заливка, ползущая
  снизу вверх через весь экран, показывает не связь с местом нажатия, а
  мельтешение. Окно суток и «Создать профиль» просто проступают целиком.

- **Появление и уход у всех окон.** Окно проступает, чуть поднимаясь и
  подрастая с 98,5 процента, затемнение набирается следом; уход — тот же
  путь назад и заметно короче: открытие человек ждёт, закрытие он уже решил
  и ждать не хочет. Величины мелкие намеренно: это признак того, что окно
  пришло, а не было всегда, а не выезд окна.

### Изменено

- **Подтверждения — отдельным окном по центру.** «Сбросить календарь»
  разворачивал вопрос прямо под кнопкой. На телефоне, где настройки открыты
  во весь экран, вопрос оказывался внизу длинной формы — иногда за краем
  экрана, и человек, нажавший кнопку, не видел вообще никакой реакции.

- **Удаление профиля переехало в настройки**, к сбросу календаря. Оно
  стояло в подвале рабочего экрана, рядом с рассказом о том, где лежат
  данные, — место было выбрано по смыслу текста, а не по тому, куда идёт
  человек. А идёт он в настройки: сброс и удаление — одна мысль «начать
  заново» на двух глубинах, и выбрать между ними, не видя обеих, было
  нельзя. У каждого свой знак и своё подтверждение с перечнем последствий.

- **Из подвала убран блок «Где лежат ваши данные».** Он повторял сказанное
  на посадочной странице и в условиях использования, а на рабочем экране,
  куда человек приходит считать часы, занимал место объяснением, которое
  читают один раз. Выгрузка профиля в файл давно есть в шапке. Внизу
  осталось то, чему там и место: тема, ссылки на условия и время последней
  записи.

- **Обводка фокуса перестала быть сигнальной.** Сигнальный цвет здесь
  значит «требует решения человека» — им помечены переработка сверх нормы,
  отказ, конфликт со сменой, — и обводить им каждое поле, до которого дошёл
  фокус, значит говорить «тут что-то не так» про обычный ввод имени. Плюс
  красная рамка на поле — общепринятый знак ОШИБКИ ВВОДА, которую тут же,
  рядом, рисует проверка. Теперь обводка цвета следа: по WCAG 2.2 она
  заметна не хуже прежней — 7,2 к 1 на поднятой бумаге против 5,1 у
  сигнального, 5,7 к 1 в тёмной теме.

- **График пишется через вертикальную черту: «1|3» вместо «1/3».** Косая
  черта читается как дробь — «одна третья», — а речь не о дроби, а о двух
  числах рядом: столько-то суток работаем, столько-то отдыхаем. Тем же
  знаком, вертикальной чертой, пара цифр разделена в названии сайта и в его
  знаке, и объяснить разнобой между вывеской и настройками было нечем.
  Поменялось везде: список графиков, подпись в шапке, показ на главной,
  свой цикл («3|1»).

  Профили, записанные раньше, открываются как были. Старое написание
  читается наравне с новым и молча заменяется новым при первой же записи —
  человек ничего не делает и ничего не замечает. Отказать такому профилю в
  чтении было нельзя: это стёрло бы человеку год внесённых отпусков из-за
  смены знака в служебной строке.

  Прежние записи этого журнала оставлены как есть: в тех выпусках график
  действительно писался через косую черту, и переписывать историю задним
  числом — врать о том, что человек тогда видел на экране.

### Исправлено

- **Окно перекашивало при закрытии.** Условием «окно открыто» был задан не
  только показ, но и направление столбца. Пока закрытие было мгновенным,
  это ничего не стоило; стоило сделать его плавным — и всё время ухода
  окно ещё показано, а условие уже снято: шапка и тело вставали в РЯД и
  разъезжались по горизонтали.

- **Окно суток опустошалось раньше, чем гасло**: содержимое исчезало в
  первый же кадр закрытия, и пустая рамка ещё десятые доли секунды таяла.

- **Страница под листом возвращалась рывком** — вспыхивала сквозь ещё
  полупрозрачный лист. Теперь возвращается тем же путём, каким уходила.

- **У правого края листа оставалась белая полоска** шириной ровно в полосу
  прокрутки: место, оставленное под неё, рисуется поверх окна. Обычному
  окну это ничего не стоило — оно с полями и до края экрана не достаёт.

- **Заголовок листа подпрыгивал** на 2,7 точки в момент нажатия: на кнопке
  слово стояло на одной высоте, а в шапке листа оказывалось выше. Теперь
  первый кадр перехода совпадает с кнопкой точка в точку.

- **Закрытие вложенного окна закрывало и то, поверх которого оно стояло.**
  «Отмена» в подтверждении сброса уносила с собой все настройки.

## [2.8.0] — 2026-08-26

Посадочная страница перестала рассказывать о приложении словами и начала
его показывать: у каждого шага своя анимация, собранная из настоящих
частей расчёта. Заодно настройки и перечень источников собрались
карточками — тем же строем, что и всё остальное.

Цифра минорная, а не заплаточная: раздел «Как работает» стал другой
вещью, а не поправленной прежней, и заплатка читалась бы как «починили
опечатку».

### Изменено

- **Раздел «Как работает» на главной стал показом, а не списком.** Была
  сетка из шести плиток со значком и строчкой текста; плитка называла
  возможность, но человек, не открывавший приложение, из неё не знал ни что
  нажимать, ни что он увидит в ответ. Теперь шагов четыре, и у каждого своя
  анимация, собранная из НАСТОЯЩИХ частей приложения: та же сетка месяца,
  те же цвета и буквы суток, та же летящая под пальцем смена, та же шапка
  и то же окно выгрузки. Выбор графика перестраивает неделю; смена
  переносится удержанием на другой день; по месяцу ложатся дни
  освобождения от работы и вызов в резерв — и видно, что действуют они
  по-разному; профиль сохраняется в файл и никуда не уходит.

  Пары, которые нельзя разрывать, объединены: график и норма — один ответ,
  освобождение от работы и вызовы стоят рядом именно потому, что действуют
  по-разному. Показ начинается, когда до него долистали, идёт по кругу —
  три секунды история, три секунды покой — и повторяется при возврате.
  Кегль раздела поднят: он идёт сразу за первым экраном, и мелкий текст
  после такого читался как сноска. В конце раздела встала та же кнопка, что
  и на первом экране: человека, дочитавшего досюда, незачем отправлять за
  ней обратно наверх.
- **Настройки собраны карточками**: слева вопрос, справа ответ, строки
  разделены линиями. Прежде подписи и поля шли сплошняком, и на телефоне,
  где панель открыта окном во всю высоту, конец одного вопроса от начала
  следующего отличался только кеглем. Теперь у столбца ответов своя ось, и
  список читается как таблица; на узком экране строка переносится, и ответ
  встаёт под вопросом сам.
- **«Полезно знать» — карточками.** Пять документов шли списком, где
  название набрано ссылкой, а пояснение под ней обычным текстом: где
  кончается один и начинается следующий, было видно только по кеглю.
  Теперь каждый на своей плашке, и нажимается вся карточка целиком, а не
  название в три слова. Значок внешней ссылки ушёл из строки заголовка в
  правый верхний угол плашки — в строке он ехал за последним словом и у
  длинных названий оказывался посреди текста. Про уход на чужой сайт
  сказано ещё и словами: значок видят не все.
- **Поля внизу страниц убраны, шаги стали ниже.** Подвал прижат к
  содержимому, а шаг раздела «Как работает» занимает половину экрана
  вместо трёх четвертей: пустоты между разделами было больше, чем самих
  разделов.

## [2.7.0] — 2026-08-26

График перестал быть чем-то, что выбирают из списка, а смена — чем-то
одинаковым на весь год. Свой цикл задаётся числами, а часы каждой смены
называются в самом дне: приложение научилось показывать не только то, как
должно было быть, но и то, как было.

### Добавлено

- **Свой график** — любое сочетание рабочих и выходных суток, помимо
  готовых 1/3, 1/4, 2/2 и пятидневки. Цикл задаётся двумя числами, от
  одних суток до тридцати одних, и считается ровно так же, как готовые:
  свой «1/3» день в день совпадает с готовым. Заданный цикл в профиле
  остаётся — переключились на 2/2 посмотреть и вернулись, он тот же.
  Продолжительность смены при этом не подставляется: у своего графика она
  ниоткуда не следует.
- **Предупреждение о сменах встык.** Цикл вроде 3/1 с суточной сменой даёт
  выход сразу после сдачи, без перерыва. Считается такое исправно, но
  вводят его чаще по ошибке — перепутав часы смены с длиной цикла, — и
  теперь под полем об этом сказано прямо.
- **Подпись месяца в производственном календаре**: рабочие дни, праздники
  и часы за предпраздничные дни — тем же строем, что и подпись месяца в
  сетке графика. Раньше рабочие дни в календаре приходилось пересчитывать
  глазами.
- **Часы отдельной смены.** Нажмите по дню со сменой: под переключателем
  появились «Начало смены» и «Продолжительность» — те же две величины, что
  и в настройках, только про одни сутки. Настройки задают график на весь
  год, а здесь говорится, что было на самом деле: сдали в одиннадцать
  вечера, отпустили в шесть, подменяли полсмены. Когда сдавать, приложение
  считает само и пишет под полями. Названные часы отменяют для этих суток и
  общее начало, и продолжительность: от начала зависит, как смена ляжет на
  двое суток, а значит и ночные часы, и то, в каком месяце они окажутся.
  Часы, совпавшие с графиком, не хранятся; смену переносят — часы едут с
  ней, снимают — уходят вместе с ней. В сутках, куда дотянулся хвост чужой
  смены, окно теперь так и говорит: часы правятся там, где смена началась.

### Изменено

- **Поле «Время отсчёта смены» называется «Начало смены».** Прежнее имя
  описывало устройство расчёта, а человек отвечает в этом поле на простой
  вопрос — во сколько он заступает.

### Убрано

- **Основание «24 часа» из нормы часов в неделю.** Возраст до шестнадцати
  лет — действительно основание ст. 92 ТК РФ, но к суточным дежурствам
  такой работник не допускается вовсе, и выбрать эту строку мог только
  тот, кто ошибся. Профили, сохранённые с ней, читаются по-прежнему —
  норма в них становится общей, сорокачасовой.

### Исправлено

- **У заглушки месяцы были скруглены круглее календаря.** Клетка скруглена
  дважды — снаружи по контуру месяца и внутри своим квадратом, — и в
  расчёте внутренний квадрат закрашивает наружную дугу обратно. У заглушки
  он был прозрачным, наружная дуга оставалась видна, и в момент подстановки
  углы месяцев заметно подбирались.
- **Смена, поставленная в сутки сдачи предыдущей, красилась бледно** — как
  продолжение, а не как заступление, и правка выглядела не сработавшей.
  Записей в таких сутках две, и клетка брала первую по порядку, то есть
  хвост вчерашней смены. Красит теперь заступление: оно событие этих суток,
  а хвост принадлежит вчерашним. Подпись клетки называет обе части и итог
  за сутки.

## [2.6.0] — 2026-08-24

Приложение перестало быть калькулятором одного графика: к суткам через
трое добавились 1/4, 2/2 и пятидневка, а продолжительность смены стала
величиной, а не константой.

### Добавлено

- **Выбор графика сменности: 1/3, 1/4, 2/2 и 5/2.** Календарь
  перестраивается под выбранный, а цифры в названии сайта меняются вместе
  с ним — старые прогорают и осыпаются, новые встают на их место. У
  графиков, где рабочих суток подряд несколько, названная дата смены — это
  ПЕРВЫЕ сутки череды: остальные достраиваются сами.
- **«5/2» строится по производственному календарю**, а не по циклу: в
  праздники смен нет, перенесённые выходные учтены, предпраздничный день
  рабочий и короче на час (ст. 95 ТК РФ). Даты смены такому графику не
  нужно, и поля у него нет. За 2026 год выходит ровно 247 смен, а факт
  сходится с нормой в ноль.
- **Продолжительность смены** — своим полем, в анкете и в настройках.
  Обычная для графика подставляется сама (24 часа для 1/3 и 1/4, 12 для
  2/2, 8 для 5/2), но правится: у двенадцатичасовых встречается
  одиннадцать с половиной, у суточных — двадцать три. От неё зависит, как
  смена делится между сутками, а значит месячные итоги и ночные на стыке
  месяцев.
- **24 часа в неделю** — новое основание нормы, абз. 2 ч. 1 ст. 92 ТК РФ,
  возраст до шестнадцати лет.

### Изменено

- Переработка «сменами» делится на свою смену, а не на сутки, и называется
  сменами: у графика «два через два» прежнее деление на 24 показывало
  вдвое меньше смен, чем человек отработал сверх нормы.

### Исправлено

- Окно дня спрашивало «есть ли тут смена» само и о выбранном графике не
  знало: у «два через два» оно отвечало неверно. Теперь этот вопрос один
  на всё приложение.
- Правка профиля в своей же вкладке не доходила до шапки: подписка ждала
  событие, которое браузер шлёт только ДРУГИМ вкладкам. Из-за этого же
  застревала надпись на кнопке первого экрана.

## [2.5.0] — 2026-08-23

Сброс наконец сбрасывает то, что накопилось; из плашки месяца ушли белые
нити, а первый экран разграфляет лист прямо на ходу.

### Изменено

- **«Сброс настроек» стал «Сбросом настроек календаря»** — и делает
  теперь то, что говорит. Раньше он возвращал к исходным имя, норму,
  время отсчёта и оба переключателя: те самые поля, что стоят тут же, над
  кнопкой, и правятся прямо в них. Нажать её значило потерять имя
  профиля, ничего при этом не решив. Теперь она снимает разом то, что
  накапливается годами и убирается только по одному в окне дня: отпуска,
  вызовы, правки видов дней, переносы и отмены смен, заметки. Настройки и
  дата рабочей смены остаются на месте.
- Кнопка первого экрана — со значком календаря, тем же, что стоит над
  сеткой внутри: человек, попавший внутрь, узнаёт кнопку, которой пришёл.
- «Бесплатно и без регистрации» написано от руки — это слово от автора, а
  не пункт регламента, и деловым шрифтом оно терялось рядом с заголовком.
  Каллиграфия, а не маркер: волосяная линия, нажим и росчерки.
- **Первый экран разграфляет лист на ходу.** Разлиновка идёт той же
  волной, что и клетки, на шаг впереди: линия под клетку дорисовывается к
  тому мгновению, когда клетка трогается. Сначала разграфили, тут же
  заполнили — одним действием. Отметки и числа сдвинуты на две десятых
  секунды, порядок истории не изменился.
- **Имя файла спрашивается при выгрузке.** Раньше оно подставлялось само —
  по имени профиля, — и файл уходил в «Загрузки» без вопросов; через
  полгода в папке лежали «Мой график.json», «Мой график (1).json» и «Мой
  график (2).json», и который из них до отпуска, было не сказать. Имя
  профиля осталось умолчанием, пустое поле не отказ.
- **Выбор темы — в шапке главной**, справа, где стояла кнопка «Открыть
  расчёт» (та была лишней: ровно та же дверь есть на первом экране). Из
  подвала переключатель убран — один и тот же орган дважды на странице
  вызывает вопрос, одно ли это и то же.
- **Окно «Создать профиль» открывается прямо на главной.** Кнопка первого
  экрана больше никуда не ведёт: раньше она уводила на страницу расчёта,
  а та, не найдя профиля, показывала ту же главную с окном поверх —
  человек нажимал кнопку и попадал на страницу, неотличимую от покинутой.
  В расчёт он теперь попадает, когда профиль создан.
- **Удаление профиля возвращает на главную.** Раньше на месте расчёта
  оставалась главная с окном поверх неё.

### Исправлено

- **Белые нити по плашке месяца.** Семь столбцов делят ширину, которая на
  семь не делится, и границы клеток попадают между пикселями; там, где
  округления соседей расходились, между ними оставался незакрашенный
  столбец, и сквозь месяц просвечивала страница. Щели закрыты подложкой,
  которая ложится под клетки, а не поверх: ни одна клетка не сдвинулась и
  не изменила размера, цветные дни не стали уже, скругления и скос уступа
  остались прежними до пикселя.
- Та же подложка на первом экране висела готовой серой решёткой ещё до
  того, как месяц начал собираться: клетки там гаснут, а подложка под
  ними — нет. Она была видна уже на первом нарисованном кадре страницы;
  теперь приходит вместе с месяцем, волной.
- **Полоса с главными числами больше не растягивается на всю ширину при
  обновлении страницы.** Мелкие итоги показываются по замеру строки, а
  первый замер шёл после отрисовки — и человек успевал увидеть два кадра
  с плашкой во весь экран.
- Отметка отпуска и отгула на первом экране появляется с подскоком, и на
  середине хода её обрезало соседней клеткой: раздувшаяся отметка уезжала
  под соседа справа и снизу. Теперь клетка с отметкой поверх соседних.

## [2.4.0] — 2026-08-23

Месяц собирается волной, числа идут барабаном, у отпуска появился
дополнительный, а у настроек — сброс.

### Добавлено

- **Дополнительный отпуск** (ст. 116-119 ТК РФ) — свой вид отсутствия, а
  не «ещё немного основного». В клетке «Д», как в табеле Т-13, свой
  цвет; в списке и в легенде стоит сразу за основным отпуском. Считается
  так же: часы по норме за эти дни из нормы исключаются.
- **Сброс настроек** — в настройках, с подтверждением. Возвращает к
  исходным имя, норму, время отсчёта смены и оба переключателя. Дата
  рабочей смены, отпуска, вызовы, правки календаря, переносы смен и
  заметки остаются: сбрасывается то, что человек назовёт заново за
  полминуты, и только оно.

### Изменено

- Месяцы в графике и в производственном календаре **собираются волной**
  наискось, как на первом экране, только вчетверо быстрее. Пятьсот клеток,
  появлявшихся разом, читались как картинка, которая была здесь всегда.
- **Цифры идут барабаном.** Знак не подменяется на месте, а приезжает со
  стороны хода: на скорости смазан, к концу встаёт резко — тем же
  замедлением, каким замедляется сам счёт. На обратном ходе барабан
  крутится в другую сторону. Так ведут себя все числа: и на первом
  экране, и в полосе итога, и в подписях месяцев.
- Окно «За какой период» — **три списка** вместо девятнадцати плиток:
  учётный период, год и месяц. У месяца есть пункт «Весь период»: раньше
  вернуться от месяца к кварталу можно было, только выбрав квартал заново.
- Смена приставки «пере» на «недо» идёт дольше и на ходу смазывается,
  становясь чёткой в конце. Четыре буквы, проезжающие свою высоту, глаз
  прочитать не успевает и не должен: пока они едут, это движение, а не
  слово.

### Исправлено

- В сомкнутом месяце местами просвечивала волосяная щель между клетками.
  Ширина сетки делится на семь не нацело, и браузер, укладывая клетку в
  целые пиксели экрана, кое-где оставлял между соседями просвет: по месяцу
  шли полосы, которых в раскладке нет. Теперь под клеткой лежит подложка
  на пиксель шире её самой.
- На светлой теме месяц первого экрана собирался почти незаметно: гребень
  волны высветлял и без того белую клетку до чистого листа, и фронт не
  проявлял месяц, а стирал его. Теперь на светлой теме гребень темнит и
  приподнимает клетку тенью, а на тёмной остаётся вспышкой, как и был.
- Числа больше не таскают за собой строку. Уходя с «32» на «8», число
  теряло знак, а вместе с ним ширину — и подпись рядом отъезжала, хотя
  двигалось соседнее число. Место под число теперь не сжимается.
- Число больше не мигает итогом перед тем, как к нему пойти: один кадр
  оно успевало показать конечное значение, отскакивало назад и только
  потом отсчитывало.

## [2.3.0] — 2026-08-23

График перестал быть только циклом: смену можно снять, назначить и
перенести. И отпуск наконец виден целиком, а не одной клеткой.

### Добавлено

- **Смену можно снять и перенести.** Цикл описывает график, а не жизнь:
  подмены и переносы случаются, и до сих пор сказать об этом приложению
  было нечем — весь год правильный, а эти сутки нет. Теперь в окне суток
  стоит тумблер «Смена в этот день»: выключен — выходной. А на самой
  сетке смену можно просто перетащить: мышью — потянуть, на телефоне —
  задержать палец, как значок на рабочем столе, и вести. Хранятся при этом
  исключения, а не переписанный график: сам цикл остаётся прежним и
  строится дальше сам.

### Изменено

- **Отпуск виден целиком, а не одной клеткой.** Освобождение с 1 по 5
  показывалось только там, где по графику стояла смена: человек видел
  «смена попала в отпуск» и не видел ни начала отпуска, ни его конца —
  хотя спор идёт ровно о границах. Теперь помечены все сутки периода,
  каким бы он ни был: отпуск, больничный, отгул, учебный отпуск. Сутки,
  свободные по графику, помечены вполголоса — тем же цветом и той же
  буквой, но слабее, как продолжение смены отличается от заступления.

## [2.2.0] — 2026-08-22

Сетка показывает то, что выбрано, целиком: «Онлайн» больше не стирает с
неё остаток периода, а производственный календарь идёт за тем же
отрезком, что и график. Расчёт и данные не тронуты — числа те же.

### Изменено

- **«Онлайн» больше не стирает будущее с сетки.** Числа наверху
  по-прежнему считаются по сегодняшний день, а сетка показывает весь
  выбранный период: сутки, которые ещё не наступили, гаснут и уходят в
  косую штриховку, но читаются полностью — где смена, где отпуск, сколько
  часов. Месяц, который весь ещё впереди, пишет над собой «ещё не
  наступил» вместо «0 см / 0,0 ч».
- **Производственный календарь показывает тот же отрезок, что и график.**
  Выбран квартал — три месяца в обеих сетках; раньше календарь показывал
  год целиком, и при переключении та клетка, из-за которой переключились,
  оказывалась в другом месте экрана.
- В календаре первого экрана у свободных суток под числом стоит «В», как
  в самом расчёте. Раньше там было пустое место, которое ничего не
  значило.

## [2.1.0] — 2026-08-22

Уход под норму показывается по-новому, и первый экран перестал врать о
норме. Расчёт и данные не тронуты: числа те же, изменилось то, как
показана их разница.

### Изменено

- Недоработка больше не отдельное число справа от переработки. Это одна и
  та же величина — разница между отработанным и нормой, — и показана она
  теперь одним числом: уходя под норму, оно краснеет, а в подписи
  приставка «пере» уезжает вниз и на её место встаёт «недо». Чисел на
  плашке всегда три, и ряд итогов больше не меняет ширину на ровном месте.

### Исправлено

- Первый экран показывал, что отпуск уменьшает норму, хотя отмеченные им
  сутки — суббота и воскресенье. Так и было: отпуск в истории шёл по
  четвёртое число, а помечались только двое суток смены, и норма падала
  из-за двух будних дней, которых на сетке не видно. Теперь отпуск лежит
  ровно на отмеченных сутках, норма при нём не меняется — как и должно
  быть, когда отсутствие пришлось на выходные, — а история кончается
  недоработкой: суточный отгул больше накопленной переработки.

## [2.0.2] — 2026-08-22

Две поломки, обе видны глазом. Вторая — на телефоне, и по ней не
открывалось ни одно окно приложения.

### Исправлено

- Скошенный уголок месяца на первом экране стоял на месте с первого кадра,
  пока весь остальной месяц ещё собирался: он лежит в пустой клетке, а
  пустые клетки в волне не участвовали. Теперь участвуют.
- Модальные окна на iPhone схлопывались до одной шапки — заголовок,
  крестик и ничего больше. Виновата была раскладка тела окна: WebKit не
  выводит высоту окна из содержимого, когда у тела нулевая основа. Это
  касалось ВСЕХ окон: и создания профиля, и настроек, и правки суток, и
  выбора периода.
- Прокрутка внутри окна, докрученная до конца, утаскивала за собой
  страницу под окном.

## [2.0.1] — 2026-08-22

Мелкая правка первого экрана. Ни расчёт, ни данные не затронуты.

### Изменено

- Первый экран рассказывает историю по очереди, а не всё разом: отметка
  отпуска проявляется и доигрывает до конца, только потом трогаются
  числа, потом так же отгул. Раньше отметка и счётчик стартовали
  одновременно и мешали друг другу — глаз не успевал поймать ни то, ни
  другое.
- Первый экран больше не растянут на всю высоту окна: его высоту задаёт
  содержимое, а на широком экране — прежний нижний предел в 84 % высоты.

## [2.0.0] — 2026-08-22

Приложение перестало быть калькулятором для пожарных и стало
универсальным: график «сутки через трое» ведут где угодно, а правила
нормы одни на всех. Вместе с ведомственной принадлежностью ушла и сверка
с выданным табелем — приложение теперь ведёт СВОЙ график, а не проверяет
чужой учёт.

### Добавлено

- Страница [условий использования](https://grafik13.ru/terms): соглашение
  и раздел «Данные и приватность» с прямым перечислением того, что лежит в
  браузере, чего нет вовсе (cookies, счётчики, сторонние скрипты) и что
  записывает веб-сервер.
- Первый экран посадочной страницы собран из настоящих деталей
  приложения: месяц графика той же сеткой, что и в расчёте, следует теме
  и собирается волной, а над ним встают три числа. Числа настоящие —
  проверка прогоняет тот же домен и сверяет каждое.
- Числа в расчёте доходят до нового значения счётчиком, а не подменяются
  мгновенно: при правке календаря видно, КАКОЕ из них двинулось. Так
  ведут себя все восемь чисел полосы итога и подписи всех двенадцати
  месяцев.
- Пока читается профиль, вместо пустого поля стоит заглушка, повторяющая
  разметку расчёта до пикселя, — страница не дёргается в момент
  подстановки.
- Кнопка первого экрана читает хранилище: «Построить график», если
  графика ещё нет, и «Открыть график», если он есть.
- Заметки к отдельным суткам: то, что человек помнит сегодня и не
  вспомнит через полгода. На расчёт не влияют.
- Несколько вызовов в одни сутки видны все, а не только первый.
- Режим «веду учёт»: расчёт обрезается сегодняшним днём, чтобы до конца
  периода не показывать «недоработку» в сотни часов.
- Переработка показывается часами или сменами и часами — по выбору.
- Почта для связи: `grafik1-3@yandex.ru`.

### Изменено

- **Домен — `grafik13.ru`** вместо прежнего.
- **Ключ профиля в браузере** переименован (`mchs-timesheet.profile` →
  `shift-schedule.profile`). Прежний ключ читается по-прежнему, и первая
  же запись переносит профиль под новое имя: делать ничего не нужно.
- **Имя файла выгрузки — имя профиля** («Основной график.json») вместо
  «табель-ГОД-караул-N.json».
- Основания недельной нормы называют статьи Трудового кодекса вместо
  ведомственных приказов. Сами числа — 40, 36 и 35 часов — не изменились.
- График строится от ЛЮБОЙ известной смены, хоть завтрашней, а не от
  первой в году: цикл четырёхдневный и достраивается в обе стороны.
- Отпуска, больничные и вызовы вносятся нажатием по самому дню в
  календаре, а не формами в боковой колонке; колонки больше нет, и её
  ширина досталась календарю.
- Учётный период и год выбираются над сеткой — там, где на них смотрят, а
  не в настройках, где год выглядел свойством человека.
- Профиль заводится тем же окном, которым потом правится: два разных
  набора одних и тех же вопросов разошлись бы.
- Итог периода — закреплённая полоса под шапкой: числа нужны при каждой
  правке в календаре.
- Часы показываются без нулевого хвоста: «16», но «15,5».
- Словарь: «Начало смены» вместо «Заступления на смену», «Освобождение от
  работы», «Работа помимо графика». Имя профиля по умолчанию — «Мой
  график».

### Удалено

- Сверка с выданным табелем вместе с полем `reported` в профиле.
- Номер караула: цикл задаёт дата смены, а номер на расчёт не влиял
  никогда.
- Северное сокращение недели как основание нормы.
- Расчёт денежных выплат и вопросы о человеке, по которым приложение
  РЕШАЛО за него его норму (статус, пол).
- Ведомственные приказы из `docs/` — на них больше ничего не ссылается.

### Исправлено

- Профиль с северным основанием показывал в настройках «40 часов», а
  считался по 36.
- Проверка после выкладки искала на страницах фразы, которых там больше
  нет: следующая выкладка упала бы на ровном месте.
- Подпись «Дата рабочей смены» в настройках указывала в пустоту — нажатие
  по ней не попадало в поле.
- Буква, набранная в поле часов, роняла весь экран расчёта вместе с
  внесёнными отпусками.
- Месячные итоги считались по дате начала смены, а не по суткам: марту
  доставались все 24 часа смены, начавшейся 31-го.
- Подвал профиля выгружал файл своей копией того же кода — имя файла
  могло разойтись с кнопкой в шапке.

## [1.0.0] — 2026-08-09

Первый выпуск. Проверка табеля суммированного учёта при графике «сутки
через трое» для аттестованных сотрудников ФПС ГПС и вольнонаёмных
работников.

[Не выпущено]: https://github.com/sp28337/mchs-manager/compare/v2.15.0...staging
[2.15.0]: https://github.com/sp28337/mchs-manager/compare/v2.14.0...v2.15.0
[2.14.0]: https://github.com/sp28337/mchs-manager/compare/v2.13.0...v2.14.0
[2.13.0]: https://github.com/sp28337/mchs-manager/compare/v2.12.0...v2.13.0
[2.12.0]: https://github.com/sp28337/mchs-manager/compare/v2.11.1...v2.12.0
[2.11.1]: https://github.com/sp28337/mchs-manager/compare/v2.11.0...v2.11.1
[2.11.0]: https://github.com/sp28337/mchs-manager/compare/v2.10.2...v2.11.0
[2.10.2]: https://github.com/sp28337/mchs-manager/compare/v2.10.1...v2.10.2
[2.10.1]: https://github.com/sp28337/mchs-manager/compare/v2.10.0...v2.10.1
[2.10.0]: https://github.com/sp28337/mchs-manager/compare/v2.9.2...v2.10.0
[2.9.2]: https://github.com/sp28337/mchs-manager/compare/v2.9.1...v2.9.2
[2.9.1]: https://github.com/sp28337/mchs-manager/compare/v2.9.0...v2.9.1
[2.9.0]: https://github.com/sp28337/mchs-manager/compare/v2.8.0...v2.9.0
[2.8.0]: https://github.com/sp28337/mchs-manager/compare/v2.7.0...v2.8.0
[2.7.0]: https://github.com/sp28337/mchs-manager/compare/v2.6.0...v2.7.0
[2.6.0]: https://github.com/sp28337/mchs-manager/compare/v2.5.0...v2.6.0
[2.5.0]: https://github.com/sp28337/mchs-manager/compare/v2.4.0...v2.5.0
[2.4.0]: https://github.com/sp28337/mchs-manager/compare/v2.3.0...v2.4.0
[2.3.0]: https://github.com/sp28337/mchs-manager/compare/v2.2.0...v2.3.0
[2.2.0]: https://github.com/sp28337/mchs-manager/compare/v2.1.0...v2.2.0
[2.1.0]: https://github.com/sp28337/mchs-manager/compare/v2.0.2...v2.1.0
[2.0.2]: https://github.com/sp28337/mchs-manager/compare/v2.0.1...v2.0.2
[2.0.1]: https://github.com/sp28337/mchs-manager/compare/v2.0.0...v2.0.1
[2.0.0]: https://github.com/sp28337/mchs-manager/compare/v1.0.0...v2.0.0
[1.0.0]: https://github.com/sp28337/mchs-manager/releases/tag/v1.0.0
