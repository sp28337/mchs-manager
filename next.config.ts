import type { NextConfig } from "next";

/**
 * Приложение не ходит в сеть.
 *
 * Прежде здесь стоял `rewrites` на бэкенд: браузер запрашивал
 * `/api/backend/...`, Next.js проксировал на FastAPI. Ни бэкенда, ни
 * запросов больше нет — расчёт и хранение переехали в браузер, чтобы
 * персональные данные (в том числе сведения о здоровье: инвалидность,
 * больничные) не покидали устройство человека.
 *
 * Отсутствие прокси — не мелочь уборки, а проверяемое свойство: если
 * когда-нибудь появится запрос наружу, он не пройдёт молча через свой
 * origin, а будет виден как обращение к чужому домену.
 */
const nextConfig: NextConfig = {
  /**
   * Статический экспорт: `next build` кладёт в `out/` готовые файлы.
   *
   * Приложению нечего делать на сервере — расчёт и хранение живут в
   * браузере, — поэтому и процесса Node в проде быть не должно. Папку
   * `out/` раздаёт любой статический хостинг, а отсутствие серверной части
   * — не удобство, а то же свойство приватности, только со стороны
   * эксплуатации: у сайта, который не исполняет код, нет и логов запросов
   * к персональным данным.
   */
  output: "export",
  reactStrictMode: true,
  experimental: {
    optimizePackageImports: ["lucide-react"],
  },
};

export default nextConfig;
