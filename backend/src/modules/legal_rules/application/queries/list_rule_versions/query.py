"""`ListRuleVersionsQuery` — `GET /legal-rules/rules/{ruleId}/versions`.

**Дополнение к `openapi.yaml`** (ADDITIVE по политике изменений
API_Conventions разд. 1). Спецификация даёт создание версии, публикацию,
пробный прогон и чтение ДЕЙСТВУЮЩЕЙ версии — но не способ узнать, какие
версии у правила вообще есть.

Без этой операции работа юриста обрывается ровно посередине: он создаёт
черновик, получает его идентификатор в ответе POST — и теряет его при
первой же перезагрузке страницы. Ни опубликовать, ни прогнать вхолостую
уже нечего, потому что назвать версию нечем. `GET .../effective-version`
не помогает: черновик по определению не действует, а именно черновики и
нужны в этом списке.

Возвращаются ВСЕ версии, включая заменённые: история редакций нормы —
это и есть предмет модуля, и правило, показывающее только текущую
редакцию, не отвечало бы на вопрос «по какой норме считали в марте».
"""

from __future__ import annotations

from uuid import UUID

from pydantic import BaseModel, ConfigDict


class ListRuleVersionsQuery(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid")

    rule_id: UUID
