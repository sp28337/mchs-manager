/**
 * Длительности, общие для нескольких мест приложения.
 *
 * --- Почему отдельным файлом ----------------------------------------------
 *
 * `COUNT_MS` — свойство счётчика чисел, и жить ему естественнее в
 * `counted-number.tsx`. Но тот помечен `"use client"`, а расписание
 * первого экрана (`hero-scenario.ts`) читает СЕРВЕРНЫЙ компонент: импорт
 * из клиентского модуля дал бы ему не число, а ссылку на клиентский
 * экспорт, и арифметика по ней молча развалилась бы.
 *
 * Поэтому длительность лежит в обычном модуле, а оба берут её отсюда.
 */

/**
 * Сколько идёт перещёлкивание числа.
 *
 * Меньше полусекунды глаз не успевает поймать, больше секунды — правка в
 * календаре начинает казаться медленной: человек ставит отметку и ждёт
 * ответа.
 */
export const COUNT_MS = 900;
